# Runtime artifacts live here. Gitignored except for this README.
#
# Created at runtime:
#   ledger.db         — SQLite ledger (verifications, traces, HITL queue, workflow runs)
#   screenshots/      — Playwright captures (one per workflow step on real Chromium runs)
